package tree.of.life.logic;

public class NPC extends Character{

    public NPC(String name, int health, int attackDamage, boolean isAlive) {
        super(name, health, attackDamage, isAlive);
    }
}
